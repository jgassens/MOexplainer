import { GuidedOrbitalLesson } from '../GuidedOrbitalLesson';
import type { LessonComponentProps } from '../types';

export function PiChainLesson(props: LessonComponentProps) {
  return <GuidedOrbitalLesson {...props} lessonId="pi-chain" />;
}
